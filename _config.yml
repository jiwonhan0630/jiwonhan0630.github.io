# _config.yml
permalink: pretty
collections:
  abouts:
    output: true
    permalink: /abouts/:slug
  works:
    output: true
    permalink: /works/:slug
  frags:
    output: true
    permalink: /frags/:slug
sass:
  sass_dir: _sass
  style: compressed # 빌드 시 CSS 파일 용량을 최소화(Minify)해줍니다.